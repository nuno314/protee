android {
    signingConfigs {
        debug {
            storeFile file('upload-keystore.jks')
            storePassword 'protee'
            keyAlias 'androiddebugkey'
            keyPassword 'protee'
        }
    }
}