import 'package:flutter/material.dart';

const boxShadowlight = [BoxShadow(blurRadius: 4, color: Colors.black12)];
final boxShadowMedium = [BoxShadow(blurRadius: 8, color: Colors.grey[400]!)];
final boxShadowDark = [BoxShadow(blurRadius: 10.9, color: Colors.grey[400]!)];

const boxShadowlightest = [
  BoxShadow(
    color: Color.fromRGBO(0, 0, 0, 0.02),
    offset: Offset(0, 4),
    blurRadius: 4,
  )
];

const blueShadow = [
  BoxShadow(
    color: Color.fromRGBO(0, 150, 178, 0.4),
    offset: Offset(0, 4),
    blurRadius: 16,
  )
];

const shadows = [
  BoxShadow(
    color: Color(0xff0097B2),
    offset: Offset(0, 4),
    blurRadius: 16,
  )
];
